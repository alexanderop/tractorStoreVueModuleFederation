{
  "name": "vue-mf-workspace",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "dev": "pnpm --parallel --filter \"apps/*\" dev",
    "build": "pnpm -r --filter \"apps/*\" build",
    "dts": "pnpm -r --filter \"apps/*\" dts"
  },
  "overrides": {
    "vue": "^3.4.0"
  }
}
